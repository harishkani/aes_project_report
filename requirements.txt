# Requirements for generating diagrams programmatically

# For Python Graphviz diagrams
graphviz>=0.20.1

# Note: You also need to install the Graphviz system package:
# Ubuntu/Debian: sudo apt-get install graphviz
# macOS: brew install graphviz
# Windows: Download from https://graphviz.org/download/
